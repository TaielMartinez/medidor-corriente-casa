MEDIDOR 2 CASAS — 1× ESP32-C3 + 2× SCT-013 (estilo Savjee / OpenEnergyMonitor)
==================================================================================

  TABLERO CASA 1                    TABLERO CASA 2
  ┌─────────────┐                   ┌─────────────┐
  │ Fase ──┐    │                   │ Fase ──┐    │
  │    [SCT-013 #1]                 │    [SCT-013 #2]
  │        │    │                   │        │    │
  └────────┼────┘                   └────────┼────┘
           │ cable 3,5 mm                      │ cable 3,5 mm
           ▼                                   ▼
      ┌─────────┐                         ┌─────────┐
      │ Jack J1 │                         │ Jack J2 │
      └───┬─────┘                         └───┬─────┘
          │  Burden Rb + R1/R2 + C 10µF       │
          │         (circuito ×2)             │
          ▼                                   ▼
      GPIO0 (ADC)                         GPIO1 (ADC)
          │                                   │
          └──────────────┬────────────────────┘
                         ▼
              ┌──────────────────────┐
              │  ESP32-C3 SuperMini  │  ← UNA SOLA PLACA
              │  EmonLib + WiFi      │
              │  LittleFS + HTTP :80 │
              │  LED rojo → GPIO2    │
              └──────────┬───────────┘

  LED rojo (estado):
    - Parpadeo  → sin WiFi / sin internet (prioridad máxima)
    - Fijo ON   → red OK, sin corriente en Casa 1 y Casa 2
    - Apagado   → red OK y hay corriente en al menos una casa
                         │ WiFi
                         ▼
                    [ Router LAN ]
                         │
              Cliente API (curl, app, etc.) → /api/status

  Salida: solo API HTTP (JSON) por WiFi — sin pantalla local

  Referencia cableado por canal (Savjee):
      Jack SCT ── Burden ── nodo ADC ── GPIO
      3.3V ── R1 ── nodo ADC ── R2 ── GND   (R1 = R2)
